function app() {
    //      FORMA DE TRABAJAR:
    // 1º   Deinir los nodos del DOM:
    // De momento sabemos solamente con 'querySelector'
    // Se guarda el objeto en una variable
    let x = document.querySelector('#nombre')

    //la principal funcion es definir manejadores de eventos
}

document.addEventListener('DOMContentLoaded', app)